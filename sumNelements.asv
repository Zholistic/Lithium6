function [ outputArray ] = sumNelements(inputArray, n)
%Takes in an array 'inputArray' and an integer 'n'. Sums successive n 
%elements in the array and outputs the new array (which will be 

j=0;
for i=1:length(inputArray)
    
    j = j+1;
    if(























outputArray = arrayFinal;


end